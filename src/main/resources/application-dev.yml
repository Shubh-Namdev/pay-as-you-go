
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/payg
    username: root
    password: root123

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  kafka:
    bootstrap-servers: localhost:9092
    listener:
      concurrency: 3

  data:
    redis:
        host: localhost
        port: 6379

