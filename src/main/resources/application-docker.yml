
spring:
  datasource:
    url: jdbc:mysql://mysql:3306/payg
    username: root
    password: root123

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  kafka:
    bootstrap-servers: kafka:9092
    listener:
      concurrency: 3

  data:
    redis:
        host: redis
        port: 6379

